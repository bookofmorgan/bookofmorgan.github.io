---
title: Placeholder
description: Internal placeholder so the blog route resolves at build time. Filtered out of all queries via draft flag. Delete once the first real post lands.
publishedAt: "2026-01-01"
draft: true
---

# Placeholder

This file exists so Turbopack can resolve the dynamic MDX import path for `/blog/[slug]`. It's filtered out by `getAllBlogPosts()` in production (`draft: true`), so it never renders.

Delete this file when the first real blog post is ready.
